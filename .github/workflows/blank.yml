name: Hello-world
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - name: build step
         runs: echo "A demo build"
     test:
      run-on: ubuntu-latest
      steps:
       - name: Test Phase
        runs: echo "Running test cases"

     deploy:
       run-on: ubuntu-latest
       steps:
         -name: deploy the app
         runs: echo "deploying the app"
