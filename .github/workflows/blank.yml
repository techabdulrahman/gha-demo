name: Hello-world
on: 
  push:
    branches: 
      - main 
      - develop
      - 'release/**'
  pull_request:
    branches: 
      - main 
      - develop
      - 'release/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "A demo build"


